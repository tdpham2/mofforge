# Requirements for pormake analysis scripts (describe_bbs.py, describe_topologies.py)
#
# pormake 0.2.x depends on pymatgen<2024 which uses numpy APIs removed in
# numpy 2.0 (np.deprecate).  The numpy<2 pin is needed because pymatgen<2024
# does not declare the upper bound in its own metadata.
#
# Install with:
#   pip install -r scripts/requirements.txt

pormake>=0.2.2
numpy>=1.24,<2
rdkit
